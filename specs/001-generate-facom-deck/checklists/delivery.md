# Requirements & Delivery Checklist: Gerar Deck FACOM/UFMS Animado a partir de CONTEXT.md

**Purpose**: Validar a qualidade e completude dos requisitos (spec.md + plan.md) nas áreas
explicitamente pedidas — schema do CONTEXT.md, identidade FACOM/UFMS, PPTX editável,
animações, notas, proveniência, renderização, overflow, overlap e auditoria OOXML — antes de
avançar para `/speckit-tasks`. Testa se os REQUISITOS estão bem escritos, não se a
implementação funciona.
**Created**: 2026-07-27
**Feature**: [spec.md](../spec.md) · [plan.md](../plan.md) · [data-model.md](../data-model.md)

**Note**: This checklist is generated by the `/speckit-checklist` command based on feature context and requirements.

## Schema e Validação do CONTEXT.md

- [ ] CHK001 - Estão especificados quais campos do frontmatter do `CONTEXT.md` são
  obrigatórios versus opcionais-com-default, de forma completa o bastante para implementar
  `context.schema.json`? [Completeness, Spec §Key Entities, Data-Model §1]
- [ ] CHK002 - O comportamento para um campo de frontmatter desconhecido está definido de
  forma inequívoca (aviso vs. erro) e consistente entre `spec.md` e `data-model.md`?
  [Consistency, Spec §Assumptions, Data-Model §1]
- [ ] CHK003 - O requisito de preservar o corpo Markdown "sem descartar ou alterar" está
  acompanhado de um critério verificável (ex.: comparação byte-a-byte, hash) ou permanece uma
  afirmação não mensurável? [Measurability, Spec §FR-003]
- [ ] CHK004 - Estão definidos os requisitos para quando `brand.profile` referencia um perfil
  de marca inexistente no catálogo? [Gap, Data-Model §1]
- [ ] CHK005 - O requisito de rejeitar formatos de tela diferentes de `ppt169` está marcado
  como decisão de escopo (Assumption) e não como um requisito funcional numerado, evitando
  ambiguidade sobre se é obrigatório implementar suporte a outros formatos? [Clarity, Spec §Assumptions]

## Identidade e Proveniência FACOM/UFMS

- [ ] CHK006 - Os requisitos de identidade visual (cor `#0088B7`, seleção positiva/negativa por
  contraste, área de proteção) estão quantificados o bastante para serem verificados
  automaticamente, e não apenas descritos qualitativamente? [Measurability, Spec §FR-005, §FR-011]
- [ ] CHK007 - O limiar exato de "mais de 40% de preenchimento escuro" que aciona a versão
  negativa da marca está definido de forma consistente entre `spec.md` (Edge Cases) e
  `data-model.md` (`brand-policy.yaml`)? [Consistency, Spec §Edge Cases, Data-Model §3]
- [ ] CHK008 - Estão especificados os campos mínimos exigidos de um registro de proveniência
  (URL/origem, data de captura, condição de uso) para TODO asset de marca, imagem externa e
  alegação factual — não apenas para os logos oficiais? [Completeness, Spec §FR-013, §SC-007]
- [ ] CHK009 - O requisito "nunca substituir silenciosamente" um asset de marca ausente está
  acompanhado de um comportamento de falha explícito e testável (código de saída, mensagem)?
  [Clarity, Spec §FR-006, §Edge Cases]
- [ ] CHK010 - As regras de co-branding/hierarquia de convivência com marcas externas (UFMS à
  direita/abaixo vs. Governo Federal à esquerda/acima, citadas em CONTEXT.md §4.2) estão
  explicitamente marcadas como fora do escopo desta slice, ou existe uma lacuna sobre se elas
  são exigidas? [Gap, Ambiguity]
- [ ] CHK011 - O requisito de que a tipografia institucional oficial não seja redistribuída
  está consistente com o requisito de que os logos oficiais sejam preservados sem alteração —
  ou existe conflito entre "preservar identidade oficial" e "usar fontes de fallback"?
  [Consistency, Spec §Assumptions]

## PPTX Nativo Editável

- [ ] CHK012 - O requisito de que o PPTX seja "nativamente editável" está definido com
  critérios objetivos (formas/texto reais vs. imagem rasterizada), ou depende de interpretação
  subjetiva de quem revisa? [Measurability, Spec §FR-007, §SC-002]
- [ ] CHK013 - Os requisitos para as três seções obrigatórias (abertura, conteúdo,
  encerramento) especificam o que cada uma deve conter, ou apenas que devem existir?
  [Clarity, Spec §FR-007]
- [ ] CHK014 - Existe um requisito explícito de que o motor de exportação existente
  (`svg_to_pptx.py`) não é modificado por esta feature, e ele está consistente entre
  `spec.md` (FR-014) e `plan.md` (Project Structure)? [Consistency, Spec §FR-014, Plan §Project Structure]

## Animações e Transições

- [ ] CHK015 - Os quatro valores de `delivery.animations` (`none`, `subtle`, `purposeful`,
  `narrative`) têm requisito de comportamento definido para todos, ou apenas para `none` e
  `purposeful` como hoje aparece em `spec.md`? [Gap, Spec §FR-009]
- [ ] CHK016 - O critério "sem referências de âncora inexistentes ou conflitantes" é
  suficientemente mensurável para servir de critério de aceitação automatizado?
  [Measurability, Spec §Acceptance Scenario US1.3, §SC-004]
- [ ] CHK017 - Está definido o que "narrative" (preset citado em CONTEXT.md §6/§9 mas ausente
  do enum de `spec.md`) deve fazer, ou essa lacuna precisa ser resolvida antes de
  `/speckit-tasks`? [Gap, Ambiguity]

## Notas do Apresentador

- [ ] CHK018 - O requisito de notas do apresentador especifica algum critério mínimo de
  conteúdo (ex.: cobrir os pontos do slide, citações quando `delivery.citations: true`), ou
  apenas que notas "existam"? [Clarity, Spec §FR-008]
- [ ] CHK019 - A relação entre `delivery.citations: true` e o conteúdo das notas do
  apresentador está definida, já que ambos aparecem no `CONTEXT.md` mas apenas notas têm um
  FR próprio? [Gap, Data-Model §1]

## Renderização Completa, Overflow e Overlap

- [ ] CHK020 - O critério "100% dos slides renderizados" define o que conta como falha de
  renderização de uma página individual (timeout, exceção, página em branco)? [Clarity, Spec §SC-002]
- [ ] CHK021 - "Overflow", "clipping" e "overlap não intencional" estão definidos com critérios
  objetivos e distintos entre si, ou tratados como sinônimos intercambiáveis? [Clarity, Spec §FR-010, §SC-002]
- [ ] CHK022 - Existe um requisito para o formato/composição exata do "contact sheet" (grade
  única vs. conjunto de PNGs individuais), ou a forma de agregação fica em aberto? [Ambiguity, Spec §FR-010]
- [ ] CHK023 - O requisito de renderização completa está consistente com a declaração de
  capacidade `browser: optional` do host (plan.md/data-model.md) — o que acontece com o gate
  de overflow/overlap quando o navegador de renderização está indisponível? [Consistency, Data-Model §5, Plan §Constitution Check]

## Auditoria OOXML e Gate de Entrega

- [ ] CHK024 - O requisito de "abertura correta em PowerPoint ou LibreOffice" especifica qual
  ferramenta é a autoridade de verificação quando ambas não estão disponíveis no ambiente de
  execução? [Gap, Spec §Assumptions]
- [ ] CHK025 - O comportamento de bloqueio rígido de exportação (FR-019 — nenhum PPTX gravado
  enquanto um gate falhar) está consistente em TODAS as três menções no spec (Clarifications,
  Edge Cases, FR-019, Acceptance Scenario US1.5)? [Consistency, Spec §Clarifications, §FR-019]
- [ ] CHK026 - Está definido o que a pessoa usuária recebe quando a exportação é bloqueada
  (apenas uma lista de falhas, ou também artefatos parciais para depuração como `svg_output/`
  e `svg_final/`)? [Gap, Spec §FR-019]
- [ ] CHK027 - O requisito "a lista exata dos gates que falharam" é mensurável — existe um
  formato/schema definido para essa lista, ou apenas uma descrição textual do requisito?
  [Measurability, Data-Model §6]
- [ ] CHK028 - Existe critério de aceitação para o caso em que TODOS os gates pré-exportação
  passam mas a exportação nativa (`svg_to_pptx.py`) falha por um motivo já conhecido do motor
  existente — o comportamento herdado está documentado como tal? [Clarity, Contracts §cli-contract.md]

## Host, Capacidades e Escopo Multimodelo

- [ ] CHK029 - Os onze campos do contrato de capacidades de host têm requisito de
  comportamento (fallback) definido individualmente, ou apenas os citados em Edge Cases
  (`browser`, `interactive_confirmation`)? [Gap, Spec §FR-015, Contracts §host-capabilities.schema.json]
- [ ] CHK030 - O escopo reduzido do GitHub Copilot (apenas interface, sem adapter funcional)
  está expresso de forma idêntica e sem contradição em `spec.md` FR-018, na seção
  Assumptions, e em `plan.md` Faseamento? [Consistency, Spec §FR-018, Plan §Faseamento]
- [ ] CHK031 - Existe um requisito equivalente de "manter Claude Code funcional sem
  regressão" (FR-012) com algum critério verificável de "sem regressão", ou o termo permanece
  não quantificado? [Measurability, Spec §FR-012]

## Dependências, Suposições e Ambiguidades Remanescentes

- [ ] CHK032 - Cada Assumption do spec.md tem uma justificativa rastreável até uma decisão do
  CONTEXT.md ou uma resposta explícita do usuário (clarify), evitando suposições não
  verificadas silenciosamente inseridas na especificação? [Traceability, Spec §Assumptions]
- [ ] CHK033 - A dependência de rede "intermitente" durante a criação do Pacote de Marca está
  acompanhada de um requisito de comportamento quando a rede está totalmente indisponível
  durante essa etapa específica (não apenas em runtime)? [Gap, Spec §Assumptions]
- [ ] CHK034 - Existe algum requisito de desempenho/tempo de geração, dado que o próprio nome
  da feature e o contexto de origem enfatizam "palestra amanhã"? Está claramente marcado como
  fora de escopo em vez de simplesmente ausente? [Gap, Plan §Technical Context]
- [ ] CHK035 - Um esquema de identificação (IDs) para requisitos e critérios de aceitação já
  está estabelecido e usado de forma consistente (`FR-XXX`, `SC-XXX`, `CHK-XXX`) em todos os
  artefatos desta feature? [Traceability]

## Notes

- Todos os itens testam a qualidade dos requisitos em `spec.md`/`plan.md`/`data-model.md` —
  nenhum item verifica comportamento de implementação, já que o código desta feature ainda não
  existe.
- CHK017 e CHK022 apontam a mesma lacuna já sinalizada como "Outstanding" no relatório de
  `/speckit-clarify" (categoria Non-Functional/format) mais duas lacunas novas encontradas
  neste checklist (preset `narrative` sem comportamento definido; formato do contact sheet).
- Recomenda-se resolver CHK001, CHK017, CHK022, CHK025 e CHK029 antes de `/speckit-tasks`,
  pois afetam diretamente a decomposição de tarefas (schema completo, presets de animação,
  formato do relatório de conformidade, contrato de capacidades por campo).
- Check items off as completed: `[x]`
- Add comments or findings inline
- Items are numbered sequentially for easy reference
